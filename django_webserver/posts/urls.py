from django.urls import path

from . import views

app_name = "posts"

urlpatterns = [
    # path("", views.IndexView.as_view(), name="index"),
    # path('<int:pk>', views.posts_view, name='posts_view'),
]